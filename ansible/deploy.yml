---

- hosts: all
  become: yes
  strategy: debug

  tasks:
    - name: Copier  le jar dans le repertoire des application de tomcat
      copy:
        src:   "{{ target_dir }}/{{ jar_file }}"
        dest:  /opt/apache-tomcat-9.0.38/webapp

        

